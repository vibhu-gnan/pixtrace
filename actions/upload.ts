'use server';

// Upload server actions
// Hero image upload was removed — cover now uses existing media via cover_media_id.
// Photo uploads use the presigned URL flow (presigned-url → R2 direct → /api/upload/complete).
